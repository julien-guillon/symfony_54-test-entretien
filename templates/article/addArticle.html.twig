{% extends 'base.html.twig' %}

{% block title %}Ajouter un article{% endblock %}

{% block body %}
<style>
    {# .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; } #}
</style>

<div class="wrapper">
    <div class="form_wrapper">
        <h1 class="form__title">Ajoutez un nouvel article !</h1>

        <div class="form">
            {{form_start(addArticleForm)}}
                {{form_widget(addArticleForm)}}
                {{ form_errors(addArticleForm) }}

                <input type="submit" class="form__btn" value="Ajouter un article">

            {{form_end(addArticleForm)}}
        </div>
    </div>

</div>
{% endblock %}