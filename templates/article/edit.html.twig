{% extends 'base.html.twig' %}

{% block title %}Modifier - {{ article.title }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <h1 class="mt-3 text-truncate">Modifier - {{ article.title }}</h1>
            <div class="my-3">
                {{ include('article/_form.html.twig', {'button_label': 'Enregistrer'}) }}
            </div>
            <div>
                <a class="btn btn-sm btn-primary" href="{{ path('articles') }}">Retour à la liste</a>
                {{ include('article/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
