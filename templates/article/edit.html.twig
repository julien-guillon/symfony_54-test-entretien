{% extends 'base.html.twig' %}

{% block title %}Editer - {{ article.title }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <h1 class="mt-3">Editer - {{ article.title }}</h1>
            <div class="mb-3">
                {{ include('article/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
            </div>
            <div>
                <a class="btn btn-sm btn-primary" href="{{ path('articles') }}">Retour à la liste</a>
                {{ include('article/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
