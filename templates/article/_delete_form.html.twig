<form method="post" class="d-inline" action="{{ path('article_delete', {'slug': article.slug}) }}" onsubmit="return confirm('Etes-vous sûr ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}">
    <button class="btn-sm btn btn-danger">Supprimer</button>
</form>
