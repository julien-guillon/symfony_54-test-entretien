{% extends 'base.html.twig' %}

{% block title %}Ajouter un article{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <h1 class="mt-3">Ajouter un article</h1>
            <div class="mb-3">
                {{ include('article/_form.html.twig') }}
            </div>
            <div>
                <a class="btn btn-sm btn-primary" href="{{ path('articles') }}">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
