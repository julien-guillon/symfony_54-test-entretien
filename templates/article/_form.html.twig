{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary btn-lg w-100 mt-2">{{ button_label|default('Enregistrer') }}</button>
    {% if (form.children | filter(c => c.vars.required) | length) > 0 %}
        {% if (form.children | filter(c => c.vars.required) | length) > 1 %}
        <p class="mt-3">* Champs requis</p>
        {% else %}
        <p class="mt-3">* Champ requis</p>
        {% endif %}
    {% endif %}
{{ form_end(form) }}
